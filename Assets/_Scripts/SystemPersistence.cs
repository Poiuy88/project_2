using UnityEngine;

public class SystemPersistence : MonoBehaviour
{
    public static SystemPersistence instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}